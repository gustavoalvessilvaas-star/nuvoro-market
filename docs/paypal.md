# PayPal

PayPal is prepared as a disabled placeholder.

Required variables:

- `PAYPAL_CLIENT_ID`
- `PAYPAL_CLIENT_SECRET`
- `NEXT_PUBLIC_PAYPAL_ENABLED=true`

Keep `NEXT_PUBLIC_PAYPAL_ENABLED=false` until the PayPal business account, sandbox testing and production review are complete.
